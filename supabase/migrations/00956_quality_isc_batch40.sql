-- Migration: Quality fix -- ISC batch 40 (30 questions)
-- Generated by generate-quality-scaffold.ts
-- Issues: #1 (giveaway longest)

-- Q12625: Change Management [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Application deployments may conflict with infrastructure changes that lack coordinated scheduling","Uncontrolled infrastructure changes may create security vulnerabilities, outages, and compliance gaps","The organization will be unable to implement a configuration management database effectively","Developers will lose access to the production environment needed for troubleshooting issues"]$EXPL$::jsonb
WHERE id = 12625;

-- Q12762: Security Controls and Monitoring [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Configuring threshold-based alerting that triggers after multiple failed attempts within a defined time window","Disabling failed login alerting entirely to eliminate the volume of analyst alerts","Requiring all employees to use password managers to prevent mistyped password entries","Routing all alerts directly to the help desk rather than the security operations team"]$EXPL$::jsonb
WHERE id = 12762;

-- Q12769: Security Controls and Monitoring [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Requiring password changes every 30 days to limit credential exposure windows","Removing SSO and requiring separate credentials for each application in the portfolio","Extending conditional access policies to evaluate additional geographic location parameters","Implementing continuous session monitoring that re-evaluates risk signals and revokes access upon anomalies"]$EXPL$::jsonb
WHERE id = 12769;

-- Q12771: Security Controls and Monitoring [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Installing antivirus software on all POS terminals to detect malicious activity","Implementing network segmentation with firewall rules restricting inter-segment traffic","Encrypting all traffic between employee workstations and POS terminal devices","Upgrading network switches to support higher bandwidth and faster throughput"]$EXPL$::jsonb
WHERE id = 12771;

-- Q12772: Security Controls and Monitoring [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["SHA-1 hashes will no longer produce fixed-length output for software patch verification","The collision vulnerability affects only password storage, not file integrity verification","An attacker could craft a malicious patch producing the same hash, bypassing integrity verification","SHA-1 collision attacks require quantum computing and remain practically unexploitable today"]$EXPL$::jsonb
WHERE id = 12772;

-- Q12778: Security Controls and Monitoring [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Column-level encryption on PHI columns, avoiding decryption overhead for non-PHI queries","TDE, because it provides stronger encryption than column-level encryption approaches","Column-level encryption on all 40 columns to maximize overall data protection coverage","TDE, because column-level encryption does not support AES-256 encryption algorithms"]$EXPL$::jsonb
WHERE id = 12778;

-- Q12782: Security Controls and Monitoring [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Network isolation in a dedicated VLAN with firewall rules, IDS monitoring, and physical access controls","Installing antivirus software on the SCADA system despite the vendor''s stated restrictions","Requiring SCADA operators to change their passwords on a mandatory 30-day rotation schedule","Implementing full-disk encryption on all SCADA workstations connected to the production line"]$EXPL$::jsonb
WHERE id = 12782;

-- Q12885: Threats, Attacks, and Vulnerabilities [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Option 1 alone, since limiting input length is sufficient to prevent exploitation","Option 2 alone, since HMAC integrity checks prevent tampering with serialized objects","Options 2 and 3 together, since integrity verification plus eliminating native deserialization provides defense-in-depth","All three options, since defense-in-depth requires implementing every available control measure"]$EXPL$::jsonb
WHERE id = 12885;

-- Q12889: Threats, Attacks, and Vulnerabilities [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Insecure design vulnerabilities","Software and data integrity failures","Broken access control weaknesses","Security logging and monitoring failures"]$EXPL$::jsonb
WHERE id = 12889;

-- Q12903: Threats, Attacks, and Vulnerabilities [easy/L1]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Injection vulnerabilities targeting input handling","Software and data integrity failures","Security misconfiguration in system settings","Cryptographic failures in data protection"]$EXPL$::jsonb
WHERE id = 12903;

-- Q12964: IT Audit Frameworks [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Incorrect; risk identification requires only threat and vulnerability catalogs, not asset inventories","Incorrect; asset valuation is only necessary during the risk treatment phase for control selection","Correct, but only when the organization uses quantitative rather than qualitative risk analysis","Correct; asset inventory is foundational to risk identification because threats cannot be assessed without knowing what assets exist"]$EXPL$::jsonb
WHERE id = 12964;

-- Q13194: Privacy Requirements and Data Protection [easy/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["No, because GDPR only applies to companies with a physical presence in EU member states","Yes, because GDPR applies extraterritorially to any organization offering goods or services to EU individuals","No, because GDPR applies only to organizations that process health or financial data categories","Yes, but only if the company''s annual revenue exceeds the EUR 10 million regulatory threshold"]$EXPL$::jsonb
WHERE id = 13194;

-- Q13200: Privacy Requirements and Data Protection [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["End-to-end security, because the privacy settings menu contains too many navigation layers","Proactive not reactive, because NovaTech failed to conduct a pre-launch risk assessment","Visibility and transparency, because users can see that the privacy settings options exist","Privacy as the default setting, because users must actively opt out rather than being protected automatically"]$EXPL$::jsonb
WHERE id = 13200;

-- Q13210: Privacy Requirements and Data Protection [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["The security principle, because aggregated health data lacks adequate protective controls","The use limitation principle, because data collected for fitness tracking was repurposed for insurance underwriting","The access principle, because users cannot view the data shared with insurance companies","The collection limitation principle, because HealthTrack gathered excessive data from the outset"]$EXPL$::jsonb
WHERE id = 13210;

-- Q13214: Privacy Requirements and Data Protection [easy/L1]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Conducting annual workforce security awareness training programs for all staff","Implementing unique user IDs and automatic logoff for systems containing ePHI","Installing physical locks and surveillance cameras in all server room facilities","Developing written contingency plans and disaster recovery policy documents"]$EXPL$::jsonb
WHERE id = 13214;

-- Q13215: Privacy Requirements and Data Protection [easy/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["The accountability principle, because the developer did not audit the new feature''s compliance","The security principle, because contact list data was not encrypted before third-party sharing","The choice/consent principle, because users were not offered the ability to decline data sharing","The access principle, because users cannot retrieve or download their shared contact lists"]$EXPL$::jsonb
WHERE id = 13215;

-- Q13287: Security and Control Frameworks [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Multiple frameworks create unnecessary complexity; COBIT alone covers all three governance requirements","Each framework serves a distinct purpose — COSO for financial controls, NIST for cybersecurity, COBIT for IT governance","The three frameworks are interchangeable, making the selection purely a matter of auditor preference","NIST CSF is mandatory for all public companies, rendering the other two frameworks optional supplements"]$EXPL$::jsonb
WHERE id = 13287;

-- Q13301: Security and Control Frameworks [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["IG3 is legally required for all organizations that process any category of sensitive data","IG3 controls target organizations facing sophisticated adversaries, matching a classified-data contractor''s threat profile","IG3 implementation eliminates the need for separate compliance with NIST SP 800-171 requirements","IG3 provides a certification equivalent to ISO 27001 for organizational security maturity"]$EXPL$::jsonb
WHERE id = 13301;

-- Q13304: Security and Control Frameworks [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["APO covers management activities that align IT strategy with business objectives, plan resources, and organize delivery","APO is a governance domain responsible for evaluating, directing, and monitoring management activities","APO handles only IT budgeting and financial management activities within the COBIT framework","APO manages day-to-day IT service delivery, operational support, and incident resolution activities"]$EXPL$::jsonb
WHERE id = 13304;

-- Q13307: Security and Control Frameworks [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Effective, because the automated match correctly rejected all fifteen mismatched invoices during testing","Effective, because application controls operate independently of the underlying IT general controls","Ineffective, because all automated matching processes require supplemental manual verification procedures","Cannot be relied upon, because the ITGC deficiency means the match logic could have been altered without authorization"]$EXPL$::jsonb
WHERE id = 13307;

-- Q13421: SOC Report Content and Structure [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["AT-C Section 205, governing examination engagements","AU-C Section 402, addressing service organization considerations","SSAE 18 / AT-C Section 320, covering service organization controls","PCAOB AS 2601, governing service organization audits for issuers"]$EXPL$::jsonb
WHERE id = 13421;

-- Q13437: Data Management and Lifecycle [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Implement role-based access controls restricting who can query the loan database","Encrypt the files in transit using TLS and require a signed data sharing agreement","Store the loan portfolio files on encrypted drives to protect data at rest","Run data quality validation checks on the files before transmitting to the vendor"]$EXPL$::jsonb
WHERE id = 13437;

-- Q13457: Data Management and Lifecycle [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["The proposal is acceptable because degaussing effectively destroys data on all types of storage media","The proposal is inadequate for the SSDs because degaussing does not affect flash memory and requires an alternative method","The proposal is inadequate for the HDDs because degaussing is effective only on solid-state storage media","The proposal should be replaced entirely with reformatting, which is sufficient for both HDD and SSD media"]$EXPL$::jsonb
WHERE id = 13457;

-- Q13460: Data Management and Lifecycle [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Data loss prevention rules that scan outbound communications and block unauthorized sensitive data transmissions","Increasing the frequency of full backup operations to preserve additional copies of sensitive data","Implementing stronger password complexity requirements for all organizational email accounts","Adding encryption at rest to the email server''s storage volumes and attached archive drives"]$EXPL$::jsonb
WHERE id = 13460;

-- Q13461: Data Management and Lifecycle [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Only if the SSDs are also reformatted before performing the cryptographic erasure procedure","Only if hardware-level encryption used validated algorithms and key management can verify complete key destruction","Only if the SSDs will be reused internally rather than released for external disposal or resale","Cryptographic erasure is never adequate for restricted-classification data regardless of implementation quality"]$EXPL$::jsonb
WHERE id = 13461;

-- Q13469: ERP and Accounting Information Systems [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Comparing record counts and dollar totals between the source export and target import","Requiring manual review and approval of each individual invoice before import","Encrypting the data file to protect its contents during network transmission","Running a duplicate detection check on all records after they are imported"]$EXPL$::jsonb
WHERE id = 13469;

-- Q13480: ERP and Accounting Information Systems [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Encrypting payroll data files during transmission between provider and ERP systems","Implementing cutoff procedures that trigger an accrual entry when batch timing crosses a reporting period boundary","Requiring dual authorization approval on every payroll journal entry before posting","Switching from weekly batch processing to real-time processing for all payroll transactions"]$EXPL$::jsonb
WHERE id = 13480;

-- Q13484: ERP and Accounting Information Systems [hard/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Lowering the VP approval threshold from $25,000 to $10,000 for all purchase orders","Implementing automated aggregation of purchase orders by vendor and department to flag cumulative totals exceeding $25,000","Requiring VP-level approval on all purchase orders regardless of individual transaction amount","Removing the automated workflow engine and reverting to manual paper-based approval routing"]$EXPL$::jsonb
WHERE id = 13484;

-- Q13487: ERP and Accounting Information Systems [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Exception reporting for data validation at system integration points","Batch control total reconciliation between connected systems","Output distribution control over generated system reports","Segregation of duties enforcement within the import process"]$EXPL$::jsonb
WHERE id = 13487;

-- Q13488: ERP and Accounting Information Systems [hard/L4]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Data interception by unauthorized parties during network transmission","Data corruption from field-mapping errors between the two systems","Unauthorized access to the file transfer server by external attackers","Man-in-the-middle attacks intercepting data on the internal network"]$EXPL$::jsonb
WHERE id = 13488;
