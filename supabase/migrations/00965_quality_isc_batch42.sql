-- Migration: Quality fix -- ISC batch 42 (9 questions)
-- Generated by generate-quality-scaffold.ts
-- Issues: #1 (giveaway longest), #3 (incomplete explanation)

-- Q13953: Security and Control Frameworks [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Whether residual risk is acceptable and the system should receive an authorization to operate","Whether additional security controls should be selected and mapped from NIST SP 800-53","Whether the system should be recategorized under the FIPS 199 impact levels","Whether continuous monitoring tools have been properly configured and validated"]$EXPL$::jsonb
WHERE id = 13953;

-- Q13955: Security and Control Frameworks [medium/L3]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Management review only requires reviewing the current risk register and treatment plans","Internal audit results are not a required input for the annual management review","ISO 27001 clause 9.3 requires multiple inputs including audit results, interested-party feedback, and improvement opportunities","Management review is optional under ISO 27001 when internal audits reveal no nonconformities"]$EXPL$::jsonb
WHERE id = 13955;

-- Q13967: SOC Engagements [easy/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["The SOC 1 report becomes invalid and must be reissued by the service auditor","A gap exists in the control environment because the service organization''s controls assume CUECs are in place","The service organization must implement the complementary controls on behalf of the user entity","No impact results because CUECs are optional recommendations rather than requirements"]$EXPL$::jsonb
WHERE id = 13967;

-- Q13968: SOC Engagements [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Revenue recognition accuracy, expense classification completeness, and balance sheet presentation","Staffing adequacy, budget compliance, and alignment with the organization''s strategic plan","Fair presentation of the system description, suitability of control design, and operating effectiveness","Compliance with GAAP standards, adequacy of internal audit function, and risk assessment completeness"]$EXPL$::jsonb
WHERE id = 13968;

-- Q15692: Change Management [easy/L1]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["A method to encrypt data transmitted between distributed application servers","A mechanism to compress database backups for faster off-site transfer","A firewall configuration that blocks unauthorized access to production systems","A group of servers operating as one logical unit with automatic failover"]$EXPL$::jsonb
WHERE id = 15692;

-- Q15718: SOC Report Content and Structure [easy/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Automated intrusion detection alerts for network anomalies","Quarterly vulnerability scanning of external-facing systems","A board-approved code of conduct with defined security roles","Encryption of all data at rest using the AES-256 standard"]$EXPL$::jsonb
WHERE id = 15718;

-- Q15719: SOC Report Content and Structure [medium/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["The service auditor has tested CSOCs and confirmed they operate effectively throughout the period","CSOCs are controls that the user entity must implement at its own location to complement services","The service auditor assumes CSOCs are operating effectively at the subservice organization without testing","CSOCs are controls expected at the subservice organization but excluded from the service auditor''s testing"]$EXPL$::jsonb
WHERE id = 15719;

-- Q15723: SOC Report Content and Structure [easy/L1]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Type I uses Trust Services Criteria while Type II relies on the COSO framework instead","Type I covers all five Trust Services Criteria while Type II addresses only Security","Type I is a general-use report while Type II is restricted to specified parties","Type I evaluates control design at a point in time while Type II adds operating effectiveness"]$EXPL$::jsonb
WHERE id = 15723;

-- Q15746: Data Management and Lifecycle [easy/L2]
-- Issues: #1
UPDATE questions SET
  choices = $EXPL$["Three years from the filing date or due date, whichever is later","One year from the date of the last recorded transaction","Ten years from the original date of document creation","Until the completion of the next scheduled audit cycle"]$EXPL$::jsonb
WHERE id = 15746;
